--- 비밀번호 암호화
    -- 단방향 Hash
        lib 설치 
        yarn add jsonwebtoken
        yarn add rand-token(문자열 길이 맞춰서 만들어주는 라이브러리)
        (참고 https://jwt.io/)
    -- server/plugins(jwt.js 파일 생성)

    
const options = {
    -- 암호 알고리즘 방식
    algorithm : 'HS256',
    -- 이름
    issure : 'ezhome',
    -- 토큰 유효시간
    //expiresIn : '30m'
}

randToken.generate(len)

// Token 값 Get
getToken(user)

// 회원 

// SECRET_KEY 변경 되면 매칭 된 Hash 암호화의 카 값도 변경 된다.
// 그래서 한번 받급 받은 Token은 env.SECRET_KEY 변수에 저장 후 불러와 사용


--- 로그인
vuetify사이트에서 UI components -> Tabs 참고
https://vuetifyjs.com/en/components/tabs/#align-with-title
    -- PassPort 사용
        (passport Lib 설치)
        passport : yarn add passport
        passport-Local : yarn add passport-local

    -- 활용 소스 참조 (server/plugins/passport.js)
    

